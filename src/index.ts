import { makeRequest } from './module';
import type { IGET, IPOST, IResponseSuccess, IResponseError } from './types';
import { RequestType, ResponseType } from './enums';

export { makeRequest, RequestType, ResponseType };
export type { IGET, IPOST, IResponseSuccess, IResponseError };
